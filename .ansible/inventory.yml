# invetory.yml
all:
  hosts:
    droid.fdh.li:
  children:
    dev-frontend:
      hosts:
        172.17.0.1:
      vars:
        ansible_ssh_user: andreas
        ansible_python_interpreter: /usr/bin/python3
